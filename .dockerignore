# Exclude unnecessary files from the build context
node_modules
npm-debug.log
Dockerfile
docker-compose.yml
.git
.gitignore
.env
.env.*
!.env.example

# Ensure pg_hba.conf and docker-entrypoint.sh are not excluded
!docker/pg_hba.conf
!docker/docker-entrypoint.sh
